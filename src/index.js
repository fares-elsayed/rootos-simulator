import React from 'react';
import ReactDOM from 'react-dom/client';
import RootOS from './RootOS';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <RootOS />
    </React.StrictMode>
);
